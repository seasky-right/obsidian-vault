每个干扰源占据的频道互不相同 -> 频道其实就是每个目标天然自带的 ID
同一个地点的误差由当地电磁环境决定 -> 同一地点误差固定

机器狗速度 v=5m/s
切频道 1s
测量一次 5s
d(G^,G)≤20m 清除
清楚时间 3+2 = 5s
移动的时候不能测量

Q1:定位结果怎么表示和评价
Q2: 已知初测结果，如何选择下一观测位置

全向源：360°
定向源：以定向方向为中心，两边各90∘范围内有信号

就 B 题这种题，最典型的论文并不是“问题 1、2、3、4 各自想一个算法然后拼起来”，而应该有一条很明显的技术主线：

> **误差几何建模 → 单目标定位优化 → 多目标在线搜索 → 更复杂环境下的鲁棒扩展**

B 题本身几乎就是按这个顺序出的。🌚

1. **摘要**  
    不讲背景废话，直接按四问写“建立了什么模型—用了什么方法—得到什么关键结论—正式测试表现如何”。尤其问题 3、4 最后有模拟器成绩，摘要里一定要放最核心的统计结果。国赛规范要求摘要原则上不超过一页，正文不设目录且正文不超过 30 页。
    
2. **问题重述 + 问题分析**  
    重述很短。真正重要的是“问题分析”，把四问之间的关系讲出来。  
    对这题可以写成：问题 1 研究含 ±1° 测向误差时如何表达定位不确定性；问题 2 研究在已有一次观测后，下一检测点应在哪里，使不确定区域尽快缩小；问题 3 把前两个模型嵌入机器狗的**主动搜索与在线决策**；问题 4 在定向源导致“无信号≠无目标”的情况下进一步扩展。
    
3. **模型假设与符号说明**  
    这部分就是我们刚才说的那种东西，控制在 1～2 页。只写真正参与建模的假设，例如机器狗运动视为平面直线运动、同一检测点示向误差固定、不同地点误差属于 [−1∘,1∘][-1^\circ,1^\circ]、清除成功阈值 20 m 等。  
    不要把题目每一句都抄成假设。
    
4. **问题 1：建立基础的“定位区域几何模型”**  
    这是整篇论文的底层模块。一次测向不是一条精确射线，而是一个夹角为 2∘2^\circ 的扇形约束；多个检测点约束求交，形成定位多边形。然后求凸多边形的直径，也就是最远顶点对距离。  
    接下来回答“以该直径为直径的圆是否一定覆盖”：这里应该给出数学判断/反例，而不是靠画图。  
    这一问的价值不是它本身，而是你后面终于有了一个量：
    
    D(P)D(\mathcal P)
    
    来描述“**现在定位得有多准**”。
    
5. **问题 2：从几何定位变成“实验设计/下一观测点优化”**  
    已经有一个检测点 S1S_1 和一个示向度，现在决定 S2S_2。核心不是“离得越远越好”，而是研究第二条测向线与第一条测向线的**交会角、基线长度、可接收范围、目标未知位置**之间的关系。  
    于是自然定义目标函数，例如
    
    min⁡S2  E[D(P(S1,S2))]\min_{S_2} \; E[D(\mathcal P(S_1,S_2))]
    
    或者最坏情况下
    
    min⁡S2max⁡D.\min_{S_2}\max D.
    
    最后得到一个“候选检测区域”。这就是后面机器狗移动时的局部导航准则。
    
6. **问题 3：全篇核心——未知多目标的在线搜索决策模型**  
    到这里问题已经不再只是摄影测量，而是典型的 **Active Search / Sequential Decision / Route Planning**。机器狗不知道有几个源、不知道位置、不知道频道，只能“走一步—测一下—更新认知—再决定下一步”。  
    这一章通常是论文最长的，应该明确拆成几个模块：**频道发现、空间搜索、单源粗定位、下一检测点选择、定位区域更新、接近目标、光学清除、已清除频道管理、终止判断**。  
    然后给一个完整状态机或流程图。算法最好写成伪代码，而不是直接贴 Python。
    
    比如整个逻辑可以抽象成：
    
    搜索未知源→发现频道→第一次测向→选择 S2→交会定位→必要时追加观测→进入20m区域→清除.\text{搜索未知源} \rightarrow \text{发现频道} \rightarrow \text{第一次测向} \rightarrow \text{选择 }S_2 \rightarrow \text{交会定位} \rightarrow \text{必要时追加观测} \rightarrow \text{进入20m区域} \rightarrow \text{清除}.
    
    更进一步，你们可以定义一个统一的“动作代价”：
    
    C=预计减少的不确定性移动时间+切频时间+检测时间,C= \frac{\text{预计减少的不确定性}} {\text{移动时间+切频时间+检测时间}},
    
    每一步选信息收益/时间成本较高的动作。这样论文的味道会比“我们用了遗传算法规划路径”强很多。
    
7. **问题 4：在问题 3 上做鲁棒扩展，而不是推倒重来**  
    定向干扰源真正带来的麻烦是：
    
    检测不到信号⇏附近不存在干扰源.\text{检测不到信号}\not\Rightarrow\text{附近不存在干扰源}.
    
    所以问题 3 中很多“无信号排除区域”的逻辑失效。问题 4 就要增加**可见性模型**或者“多方向重复观测机制”，让机器狗从不同方位进入同一区域，避免站在定向源背面时错误排除。  
    这样问题 4 写成问题 3 的 generalization，论文会很顺，而不是突然出现另一套完全不同的算法。
    
8. **仿真、敏感性分析与模型评价**  
    不只是把正式测试三行数据贴进去。演练阶段应该统计清除率、平均单源时间、总路径长度、检测次数、切频次数等；然后做几组消融，例如“有/无问题 2 的检测点优化”“不同搜索步长”“不同定位精度阈值”，证明为什么最后选这套参数。  
    最后再放三次正式测试结果，因为正式测试只有三次，**它只能是最终展示，不能承担全部模型验证功能**。
    

所以最后整篇论文的结构大概会长这样：

**摘要 → 问题重述 → 问题分析 → 假设与符号 → Q1定位误差几何模型 → Q2最优观测点模型 → Q3在线搜索定位清除模型 → Q4定向源鲁棒扩展 → 仿真与敏感性分析 → 模型评价与改进 → 参考文献 → 附录。**

其中真正的篇幅权重，我会大致给成：

Q1:15%,Q2:15%,Q3:40%,Q4:20%,验证评价:10%\boxed{ Q1:15\%,\quad Q2:15\%,\quad Q3:40\%,\quad Q4:20\%,\quad 验证评价:10\% }

尤其你可以把这题理解成：**Q1 给“尺子”，Q2 给“局部最优动作”，Q3 用它们组成完整策略，Q4 给策略加鲁棒性。**

这其实就是很标准、也很好写成一篇完整国赛论文的结构。问题 3 才是最值得你们现在集中精力搭骨架的地方。