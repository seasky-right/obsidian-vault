在计算 $(x,y) \to (0,0)$ 的二元极限时，夹逼定理放缩的核心目标通常是**消去分母**或者**将复杂的项放大为互不干扰的一元无穷小**。考试中最常考、最好用的高频不等式有以下 4 类：

#### 1. 齐次分式有界项（最常用）

这类不等式利用分母大于等于分子中某一部分的性质，直接将分式整体放缩为 1。

- $\frac{x^2}{x^2+y^2} \le 1$ 及 $\frac{y^2}{x^2+y^2} \le 1$
    
- $\frac{|x|}{\sqrt{x^2+y^2}} \le 1$ 及 $\frac{|y|}{\sqrt{x^2+y^2}} \le 1$
    

#### 2. 均值不等式变形（AM-GM）

专门用来处理分子上带有 $xy$ 乘积，需要与分母的平方和进行对抗的场景。

- $|xy| \le \frac{1}{2}(x^2+y^2) \implies \frac{|xy|}{x^2+y^2} \le \frac{1}{2}$
    
- $\frac{|xy|}{\sqrt{x^2+y^2}} \le \frac{1}{2}\sqrt{x^2+y^2}$
    

#### 3. 三角函数与超越函数放缩

利用函数自身的有界性或一元微积分中的基本不等式。

- $|\sin(f(x,y))| \le 1$ 及 $|\cos(f(x,y))| \le 1$ （强行剥离震荡项，使其变为常数 1）
    
- $|\sin u| \le |u|$ （当 $u \to 0$ 时）
    
- $|\arctan u| \le |u|$
    

#### 4. 三角不等式

常用于处理分子分母中带有加减法、绝对值的拆分。

- $|x \pm y| \le |x| + |y|$
    
- $\left| |x| - |y| \right| \le |x - y|$


对于第一类曲面积分 $\iint_{\Sigma} f(x,y,z) dS$，什么时候用参数化、怎么用参数化，可以总结为以下标准的“大题操作指南”。

## 一、 什么时候用参数化？（判定标准）

不要凭空去想象几何体，直接看题目给出的**曲面 $\Sigma$ 的代数方程特征**：

### 1. 投影法（常规法）的适用场景

如果曲面方程能够写成显式函数 **$z = f(x,y)$**（或 $y=f(x,z)$、$x=f(y,z)$），且满足：

- 往对应的坐标面投影后，影子是一个**实心**区域。
    
- 偏导数 $z_x, z_y$ 在边界处**不会趋于无穷大**。
    
- **典型代表**：平面、抛物面（如 $z = x^2+y^2$）、不跨越边界的单侧面。
    

### 2. 参数化法（秒杀法）的强制触发点

如果遇到以下两类方程，不用犹豫，直接上参数化：

- **特征一：全柱面方程 $x^2+y^2=R^2$（或 $y^2+z^2=R^2$ 等）**
    
    - _为什么不用投影_：往 $xy$ 面投影会退化成一条线；往 $yz$ 面投影虽然是长方形，但解出 $x = \pm\sqrt{R^2-y^2}$ 会有两层皮，且在边界 $y=\pm R$ 处偏导数趋于无穷大，导致出现难算的广义积分。
        
- **特征二：全球面方程 $x^2+y^2+z^2=R^2$（或大半个球面）**
    
    - _为什么不用投影_：解出 $z = \pm\sqrt{R^2-x^2-y^2}$ 同样存在上下两层皮，且在圆盘边界处拉伸系数分母为 0，硬算极易卡壳。
        

## 二、 参数化怎么用？（三步机械化流水线）

参数化的核心任务，是把空间变量 $(x,y,z)$ 用两个独立的平面新参数（通常是角度或高度）锁死，直接拼装成一个普通的二重积分。

### 第一步：套用标准空间坐标系，让方程锁死常数

根据图形对称性，写出对应的空间坐标变换公式。把公式代入曲面方程，**直接废掉半径变量（使其退化为具体常数）**，提取出剩下的两个自由参数。

- **如果是柱面（选用柱坐标变形）**：
    
    套用 $x=\rho\cos\theta, y=\rho\sin\theta, z=z$。
    
    代入 $x^2+y^2=R^2 \implies \rho = R$（锁死）。
    
    - **最终得到的参数方程**：
        
        $$\begin{cases} x = R\cos\theta \\ y = R\sin\theta \\ z = z \end{cases}$$
        
        （可变参数仅剩：$\theta$ 和 $z$）
        
- **如果是球面（选用球坐标变形）**：
    
    套用 $x=r\sin\phi\cos\theta, y=r\sin\phi\sin\theta, z=r\cos\phi$。
    
    代入 $x^2+y^2+z^2=R^2 \implies r = R$（锁死）。
    
    - **最终得到的参数方程**：
        
        $$\begin{cases} x = R\sin\phi\cos\theta \\ y = R\sin\phi\sin\theta \\ z = R\cos\phi \end{cases}$$
        
        （可变参数仅剩：天顶角 $\phi$ 和 方位角 $\theta$）
        

### 第二步：代入被积函数，消去 $x, y, z$

把第一步得到的参数方程直接塞进题目要求积的 $f(x,y,z)$ 里面。

- _例如_：题目要求积 $f = y^2$，柱面参数化后直接改写为 $(R\sin\theta)^2 = R^2\sin^2\theta$。此时被积函数已经完全变成了新参数的表达式。
    

### 第三步：替换面积微元 $dS$ 并写出独立上下限

这一步不需要每次都去算复杂的广义雅可比行列式，考研和期末考中高频出现的标准几何体，其 $dS$ 属于基础常识，直接背诵套用：

#### 常见几何体参数化无脑秒杀表

|**曲面形态**|**参数化方程**|**面积微元 dS 的替换式**|**参数上下限判定方法**|
|---|---|---|---|
|**圆柱侧面**<br><br>  <br><br>$x^2+y^2=R^2$|$x=R\cos\theta$<br><br>  <br><br>$y=R\sin\theta$<br><br>  <br><br>$z=z$|$$dS = R \, d\theta dz$$|$\theta$：看绕 $z$ 轴转了多少圈（完整一圈即 $[0, 2\pi]$）<br><br>  <br><br>$z$：看题目给出的切削平面（如 $1 \le z \le 2$）|
|**完整球面**<br><br>  <br><br>$x^2+y^2+z^2=R^2$|$x=R\sin\phi\cos\theta$<br><br>  <br><br>$y=R\sin\phi\sin\theta$<br><br>  <br><br>$z=R\cos\phi$|$$dS = R^2\sin\phi \, d\phi d\theta$$|$\phi$（与 $z$ 轴正向夹角）：完整球面为 $[0, \pi]$，上半球面为 $[0, \frac{\pi}{2}]$<br><br>  <br><br>$\theta$（在 $xy$ 面的投影角）：完整一周为 $[0, 2\pi]$|

把上述三部分直接拼接，原本的空间曲面积分就变成了一个躺在矩形区域上的标准平面二重积分：

$$\iint_{\text{参数范围}} f(\theta, z) \cdot (\text{对应的 } dS \text{ 替换项}) \, d\theta dz$$

接下来的计算就完全回归到普通的二元定积分。


### 二、 如果非要强行用格林公式，怎么转？

如果你在考场上就是想触发格林公式，你需要利用微分几何中**单位圆周上 $ds$ 与 $dx, dy$ 的特殊代数纽带**进行强转。

在单位圆 $x^2+y^2=1$ 上（取逆时针正方向），参数方程为 $x=\cos\theta, y=\sin\theta$，我们可以得到：

- $dx = -\sin\theta d\theta = -y d\theta$
    
- $dy = \cos\theta d\theta = x d\theta$
    

我们尝试拼凑一个组合：

$$-ydx + xdy = -y(-yd\theta) + x(xd\theta) = (x^2+y^2)d\theta = 1 \cdot d\theta$$

而在参数方程下，弧长微元 $ds = \sqrt{x'^2+y'^2}d\theta = 1 \cdot d\theta$。

由此我们得到了单位圆限定的转换铁律：

$$ds = -ydx + xdy$$

现在，我们把化简后的原式 $\oint_{C} 2 ds$ 强行跨界转换成第二类曲线积分：

$$\oint_{C} 2 \, ds = \oint_{C} -2y \, dx + 2x \, dy$$

此时，它完全满足了格林公式的形式。令 $P = -2y, Q = 2x$，对其在单位圆盘 $D$ 上使用格林公式：

$$\oint_{C} -2y \, dx + 2x \, dy = \iint_{D} \left( \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right) dxdy = \iint_{D} \left( 2 - (-2) \right) dxdy = \iint_{D} 4 \, dxdy$$

利用二重积分几何意义，常数 4 提出来，剩下单位圆盘的面积 $\pi \cdot 1^2 = \pi$：

$$4 \times \pi = 4\pi$$

- **找“天花板”和“地板”的交线：** 如果曲面是由两个方程夹出来的（比如上方是 $z = 1 - x^2 - y^2$，下方是 $z = 0$，或者是另一个曲面 $z = x^2 + y^2$）。
    
- **强制令两个 $z$ 相等（这就是所谓的消去 $z$）：**
    
    把天花板和地板的方程连立起来。
    
    比如 $1 - x^2 - y^2 = 0$，或者 $1 - x^2 - y^2 = x^2 + y^2$。
    
    解出来的那个只含有 $x$ 和 $y$ 的方程，就是立体图形最宽处的“腰围”，也就是投影在地上的**影子边界**。

### 4. 考场防御总结（机械化口诀）

你在做重积分或线面积分时，如果最后剩下了三角函数的积分，请按以下步骤闭眼判定：

1. **看幂次**：如果是**偶次方**（如 $2, 4$），数出区间跨越了几个 $\frac{\pi}{2}$，直接当成几倍的点火公式。
    
2. **看区间与对称**：如果是**奇次方**（如 $1, 3$）：
    
    - 积分区间是整圈 $[0, 2\pi]$ ➡️ **直接等于 0**。
        
    - 积分区间是半圈 $[0, \pi]$ ➡️ $\sin$ 的奇次方**等于 2 倍**；$\cos$ 的奇次方**直接等于 0**。


简而言之，**涉及“定性”（敛散性、收敛半径）时，$n$ 从几开始无所谓，可以自由平移；涉及“定量”（求和函数、求导积分、泰勒展开）时，$n$ 的具体数值生死攸关，绝对不能搞错。**

## 一、 什么时候 $n$ 的起始值至关重要（不能乱动）

### 1. 求级数的和或求“和函数”时

少一项、多一项，或者 $n$ 的起点变了，算出来的函数解析式完全不同。

- **从 $n=0$ 开始：** $\sum_{n=0}^{\infty} x^n = 1 + x + x^2 + \dots = \frac{1}{1-x}$
    
- **从 $n=1$ 开始：** $\sum_{n=1}^{\infty} x^n = x + x^2 + x^3 + \dots = \frac{x}{1-x}$
    

### 2. 幂级数在逐项求导时

求导会使常数项消失，从而可能改变起始项的 $n$ 值。

- 设 $S(x) = \sum_{n=0}^{\infty} a_n x^n = a_0 + a_1 x + a_2 x^2 + \dots$ （最低次项是常数项 $a_0$）
    
- 两边求导：$S'(x) = 0 + a_1 + 2a_2 x + \dots = \sum_{n=1}^{\infty} n a_n x^{n-1}$
    
- **注意：** 这里的起点必须从 $n=0$ 变成 $n=1$，因为原来的 $n=0$ 项是常数，求导后变成 0 了。如果继续写 $n=0$，通项里会出现 $0 \cdot a_0 \cdot x^{-1}$，虽然结果也是 0，但在数学表达上不严谨且容易引发混乱。
    

### 3. 使用标准麦克劳林展开式时

背诵的标准展开式都有固定的经典起点，做题时必须对齐。

- 例如：$\sin x = \sum_{n=0}^{\infty} (-1)^n \frac{x^{2n+1}}{(2n+1)!}$，这里的 $n$ 必须从 0 开始，代入才能得到第一项 $x$。
    

## 二、 什么时候 $n$ 可以直接平移/改变（无所谓）

### 1. 判断级数的敛散性（审敛法）

根据级数的性质：**改变、增加或去掉级数的有限项，不改变级数的敛散性。**

- 比如判断 $\sum_{n=1}^{\infty} \frac{1}{n^2}$ 是否收敛，这个 $n$ 从 1 开始、从 10 开始还是从 100 开始，结论都是**收敛**（只是无限项相加的总和变了，但它依然是个有限的常数）。
    

### 2. 求收敛半径 $R$ 和收敛域

收敛半径是通过比值法 $\lim_{n \to \infty} \left|\frac{a_{n+1}}{a_n}\right|$ 或根值法算出来的。

- 极限考察的是 $n \to \infty$ 时的趋势，前面的有限项对极限结果毫无影响。因此，无论级数是从 $n=0, 1$ 还是 $n=5$ 开始，**收敛半径 $R$ 和开区间 $(-R, R)$ 绝不会发生变化**。
    

## 三、 核心技巧：如何正确进行“指标平移”？

在求和函数时，我们经常需要把两组 $n$ 的起点不同的级数合并，这就需要**指标平移（换元法）**。

> **铁律：** 无论怎么平移，**展开后的第一项必须保持完全一致**。

### 示例：把 $\sum_{n=1}^{\infty} n a_n x^{n-1}$ 的 $\sum$ 底部变成从 $k=0$ 开始

1. **令新计数器** $k = n - 1$。
    
2. **改上下限**：当 $n = 1$ 时，$k = 1 - 1 = 0$；当 $n = \infty$ 时，$k = \infty$。
    
3. **改通项**：将原通项里所有的 $n$ 都替换为 $n = k + 1$。
    
4. **代入变形**：
    
    $$\sum_{n=1}^{\infty} n a_n x^{n-1} \Longrightarrow \sum_{k=0}^{\infty} (k+1) a_{k+1} x^k$$
    

最后把字母 $k$ 重新写回 $n$，就完成了平移：$\sum_{n=0}^{\infty} (n+1) a_{n+1} x^n$。检查一下，两边展开的第一项都是 $a_1$，说明平移正确。

#### 陷阱 1：逐项求导时，$n$ 的起点必须拔高

当你使用求导法来展开函数时，常数项微分后会变成 0，此时级数的起点 **$n$ 必须往后挪一项**。

- **正确示范：**
    
    已知 $\frac{1}{1-x} = \sum_{n=0}^{\infty} x^n = 1 + x + x^2 + \dots$
    
    两边求导：
    
    $$\frac{1}{(1-x)^2} = \sum_{n=1}^{\infty} n x^{n-1} = 1 + 2x + 3x^2 + \dots$$
    
    _注意：起点必须从 $n=0$ 变成 $n=1$。如果继续写 $n=0$，通项里会出现 $0 \cdot x^{-1}$ 这种错误形式。_
    

#### 陷阱 2：两项合并时，必须对齐“首项的幂次”

当一个复杂的有理分式被你拆成两部分分别展开后，最后一步通常需要把两个 $\sum$ 合并成一个。**只有当两个级数展开后的第一项的 $x$ 幂次完全相同时，才能直接相加。**

- **典型错误：**
    
    想把 $\sum_{n=0}^{\infty} x^n$ 和 $\sum_{n=0}^{\infty} x^{n+1}$ 合并。你不能直接写成 $\sum_{n=0}^{\infty} (x^n + x^{n+1})$。
    
- **正确做法：**
    
    必须先通过**移指标**，把两者的通项都写成 $x$ 的同一次方（比如都凑成 $x^n$），再合并系数。

把 $df = Pdx + Qdy$ 展开，按照全微分的基本公式进行拼凑。

最常用的高频全微分公式就这几个：

- $x\,dx = d\left(\frac{1}{2}x^2\right), \quad y\,dy = d\left(\frac{1}{2}y^2\right)$
    
- $y\,dx + x\,dy = d(xy)$
    
- $2xy\,dx + x^2\,dy = d(x^2y)$
    
- $y^2\,dx + 2xy\,dy = d(xy^2)$


### 化为第一类曲面积分 + 对称性（你漏掉的必杀技）

**适用场景：** 曲面是**球面**或**圆柱面**，且不能用高斯公式（或者题目限制不用高斯）。

**为什么这类题绝对不能用合一投影法？**

假设曲面是半球面 $z = \sqrt{a^2-x^2-y^2}$。如果你硬套上面的投影法，去求偏导数 $z_x$ 和 $z_y$，你会带出一大堆带有根号的分母（比如 $z_x = \frac{-x}{\sqrt{a^2-x^2-y^2}}$）。代入积分后，算式会极其恐怖，考场上极易算崩。

**破解之道：** 利用方向余弦，把第二类积分转成第一类积分。

$$\iint_{\Sigma} P dydz + Q dzdx + R dxdy = \iint_{\Sigma} (P \cos\alpha + Q \cos\beta + R \cos\gamma) dS$$

球面和柱面的方向余弦是极其简单的**现成结论**：

- 例如，中心在原点的球面 $x^2+y^2+z^2=a^2$，其外法线方向余弦直接就是：
    
    $$\cos\alpha = \frac{x}{a}, \quad \cos\beta = \frac{y}{a}, \quad \cos\gamma = \frac{z}{a}$$
    

**秒杀实战：**

代入方向余弦后，你面对的就是一个第一类曲面积分。这时候，你可以直接启动第一类积分的“奇偶对称性”**和**“轮换对称性”，大量消灭被积函数中的奇次项（如 $x^3, x y^2$ 直接为 0），或者利用重心公式直接得出结果。全程不需要算任何恶心的投影和偏导。

在期末考试中，斯托克斯公式（Stokes' Theorem）的应用题目套路非常固定。你提到的“两曲面交线”是绝对的霸主，占据了至少 $80\%$ 的考题。

我们先把你最头疼的“两曲面交线”题型彻底拆解成一套**无脑执行的流水线**，然后再盘点另外三种高级考法。

### 第一部分：绝对核心——“两曲面交线”的标准解法

这类题的标志性长相：计算空间曲线积分 $\oint_L Pdx + Qdy + Rdz$，其中 $L$ 是曲面 A 和曲面 B 的交线。

**破解核心：永远用那个“平坦的截面”去替换原本复杂的空间曲线。**

下面是考场上直接套用的 4 步流水线，我们以一道经典真题为例：

**【例】** 计算 $\oint_L y dx + z dy + x dz$，其中 $L$ 为圆柱面 $x^2+y^2=a^2$ 与平面 $x+y+z=0$ 的交线，从 $z$ 轴正半轴看去为逆时针方向。

#### Step 1: 算旋度（被积函数的替换）

先不管曲线，直接计算向量场 $\mathbf{F} = (P, Q, R)$ 的旋度 $\nabla \times \mathbf{F}$。

$$\nabla \times \mathbf{F} = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ P & Q & R \end{vmatrix}$$

_在例题中：_

$$\nabla \times \mathbf{F} = \begin{vmatrix} \mathbf{i} & \mathbf{j} & \mathbf{k} \\ \frac{\partial}{\partial x} & \frac{\partial}{\partial y} & \frac{\partial}{\partial z} \\ y & z & x \end{vmatrix} = (-1, -1, -1)$$

这就是你斯托克斯转化后的新被积函数。

#### Step 2: 选平面（找平底锅）

题目给了两个面，通常是一个柱面（或球面、锥面）和一个平面。你的积分曲面 $\Sigma$ **必须选择那个平面**（被柱面切出来的那一块）。

_在例题中：_ 选平面 $\Sigma: z = -x-y$。边界就是柱面 $x^2+y^2=a^2$。

#### Step 3: 定法向（右手定则）

根据题目要求的曲线方向，用右手定则判断平面的法向量。

_在例题中：_ 从 $z$ 轴正向看为逆时针，右手四指顺着逆时针转，大拇指朝上。所以平面 $\Sigma$ 的法向量应该取**上侧**（即与 $z$ 轴正向成锐角，$z$ 分量为正）。

平面方程 $x+y+z=0$ 的单位法向量为 $\mathbf{n} = \pm \frac{1}{\sqrt{3}}(1, 1, 1)$。为了让 $z$ 分量为正，取 $\mathbf{n} = \frac{1}{\sqrt{3}}(1, 1, 1)$。

#### Step 4: 化二重积分（投影收割）

将旋度与法向量点乘，再乘以平面的面积微元 $dS$（或者直接用一合投影法投影到坐标面）。

_在例题中：_

$$\iint_{\Sigma} (\nabla \times \mathbf{F}) \cdot d\mathbf{S} = \iint_{\Sigma} (-1, -1, -1) \cdot \frac{1}{\sqrt{3}}(1, 1, 1) \, dS = \iint_{\Sigma} \frac{-3}{\sqrt{3}} \, dS = -\sqrt{3} \iint_{\Sigma} dS$$

这时候直接把 $\Sigma$ 投影到 $xOy$ 面上，投影区域 $D$ 就是圆 $x^2+y^2 \le a^2$。

因为平面 $z = -x-y$ 的面积微元 $dS = \sqrt{1 + (-1)^2 + (-1)^2} dxdy = \sqrt{3} dxdy$。

所以原式 $= -\sqrt{3} \iint_D \sqrt{3} dxdy = -3 \iint_D dxdy = -3 \times (\pi a^2) = -3\pi a^2$。

你看，一条极度恶心的空间椭圆曲线积分，最后就变成了算一个圆的面积。

为了让你更直观地理解“圆柱面与平面相交”时，斯托克斯公式是如何将边界曲线（锅沿）转化为内部倾斜截面（平底锅）的，可以通过下面的交互组件观察：

### 第二部分：除了交线题，还会怎么出？

如果你掌握了上面的流水线，你已经能拿下一大半的分数。剩下的 $20\%$，命题老师通常会在这三个方向上做文章：

#### 考法二：不闭合的空间曲线（补线法）

- **出题特征：** 题目让你求空间曲线积分，但你发现这条曲线有起点和终点（比如：从点 $A(1,0,0)$ 沿某曲线到点 $B(0,1,1)$），没有闭合成一个圈。
    
- **你的动作：** 先算旋度。如果旋度非常简单（比如常数），这绝对是考你“补线”。
    
    1. 连结一条从 $B$ 直接回到 $A$ 的最简单的空间直线段 $L_{BA}$，凑成闭合曲线 $L + L_{BA}$。
        
    2. 对整个闭合圈套用斯托克斯公式（化为平面的面积分）。
        
    3. 算出总结果后，减去你补的那条直线段的积分（$\int_{L_{BA}}$，直线积分极度好算，直接参数化 $x=1-t, y=t, z=t$ 代入即可）。
        

#### 考法三：空间路径无关（散度为0的高配版）

- **出题特征：** 曲线极其扭曲、恶心（甚至带着 $e^{\sin x}$ 这种无法积分的项），且不闭合。
    
- **你的动作：** 算旋度！如果你发现 $\nabla \times \mathbf{F} = (0,0,0)$，这题就变成了“空间积分与路径无关”。
    
    1. 这意味着向量场是“保守场”（有原函数/势函数 $u(x,y,z)$）。
        
    2. 你不需要补线，也不需要斯托克斯公式的面积分。你可以直接**凑全微分**找出 $u(x,y,z)$，然后代入起点和终点求差：$u(B) - u(A)$。
        
    3. 或者，抛弃那条恶心的曲线，用分别平行于 $x, y, z$ 轴的三条简单折线从 $A$ 连到 $B$，沿着折线硬算（因为线上另外两个变量是常数，微分为 $0$，瞬间大量清零）。
        

#### 考法四：倒用斯托克斯（换面法）

- **出题特征：** 这是一道**求第二类曲面积分**的题，不仅曲面 $\Sigma$ 极度扭曲没有底，而且被积函数长得非常复杂，高斯公式由于没有闭合也无法直接用。但是！它的边界曲线是一个非常漂亮的平面曲线（比如就在 $z=0$ 面上的一个单位圆）。
    
- **你的动作：** 仔细观察被积函数，如果它刚好能凑成某个向量场的旋度（或者题目直接写了求 $\iint_\Sigma (\nabla \times \mathbf{F}) \cdot d\mathbf{S}$）。
    
    1. 根据斯托克斯公式，旋度曲面积分**只认边界不认面**。
        
    2. 直接把那个极度扭曲的曲面 $\Sigma$ 扔进垃圾桶。
        
    3. 换成以同一条边界（比如 $z=0$ 上的圆）为基础的**最简单的平底锅（平面 $z=0$ 的圆盘）**。在新平面上重新算一次面积分，直接秒杀。
        

**总结实战策略：**

空间曲线积分 $\rightarrow$ 必须先算**旋度**。

- 旋度 $= 0$ $\rightarrow$ 找原函数 / 走平行折线（路径无关）。
    
- 旋度 $\neq 0$ 且曲线闭合 $\rightarrow$ 找截面，投影，算二重积分（流水线操作）。
    
- 旋度 $\neq 0$ 且曲线不闭合 $\rightarrow$ 补一条直线段凑封闭，用完斯托克斯再减去直线的积分。