
**汇报日期：2026 年 7 月 15 日**

## 一、当前总体进度

目前已基本打通赛题平台的完整技术流程：

**代码开发 → CI/CD 构建 → 镜像制品生成 → IEC 61499 功能块绑定 → 系统部署 → 仿真运行 → 成绩生成 → 候选结果提交**

平台前期存在的构建、制品显示和部署问题目前已基本绕过，不再作为本阶段重点。现阶段的主要任务已从“跑通平台”转向“完善调度算法并提高困难场景稳定性”。

## 二、已经完成的工作

### 1. 完成平台流程验证

已完成以下环节：

- 阅读赛题 PRD、平台使用流程及新版代码模板。
- 确认软件工厂代码仓库、构建文件和 CI/CD 使用方式。
- 成功构建自定义 C++ Scheduler 镜像。
- 在系统设计中完成制品与功能块、部署节点的绑定。
- 成功以参赛队方案启动仿真。
- 确认仿真运行的是我方代码，而非始终回退到平台内置算法。
- 仿真结果可以进入提交管理，作为正式候选 run。

### 2. 完成平台通信链路

已经跑通 Scheduler 与平台后端之间的货物状态通信：

- 读取当前仿真的货物队列。
- 根据货物 ID 上报状态。
- 支持以下完整状态链：

```
picking → picked → delivering → placing → done
```

- 正确使用运行环境中的：

```
TEAM_ID
SF_CI_WEBHOOK_TOKEN
ACOPLAT_BACKEND
```

- 已确认平台公开接口主要是 cargo API，AGV 移动、抓取和释放由平台内部动作适配器执行，参赛程序不必直接调用底层动作端口。

### 3. 完成基础调度算法

当前版本已实现：

- 仅由 `Scheduler.Global` 修改货物状态，其他角色保持被动，避免多个功能块重复调度。
- 对待处理货物进行排序。
- 根据 AGV 已分配任务数进行负载均衡。
- 加入货架亲和度和传送带亲和度。
- 处理仿真 run 切换及状态重置。
- 对 HTTP 请求加入重试与错误日志。
- 按顺序推进货物状态，能够稳定触发平台内部运输流程。

当前代码已不再等同于平台内置的固定调度策略，仿真成绩变化证明自定义算法已实际生效。

## 三、仿真测试结果

### Demo Easy

自定义基础调度器连续三次结果基本一致：

```
T ≈ 10.02～10.06 s
Sn ≈ 42.82～42.84
```

说明当前算法在简单场景中具有较好的可重复性。

平台提交管理中还存在一个更优的简单场景结果：

```
T = 8.64 s
Sn = 43.70
```

该结果目前被设置为最终提交。

### Demo Hard

已取得一次完整成功结果：

```
T = 24.04 s
Sn = 53.55
```

这证明当前代码具备处理 8 件货物困难场景的基本能力。

但后续测试中出现过：

```
sim-agv-2 与 sim-agv-3 碰撞
Sn = 0
```

因此当前困难场景的主要问题不是任务无法完成，而是多车并发调度的稳定性不足。

## 四、目前算法存在的问题

当前代码根据固定时间间隔推进 cargo 状态，但没有持续读取 AGV 的真实物理状态。

这会造成以下问题：

- cargo 已上报 `done`，但 AGV 可能仍在移动或尚未离开货架区域。
- 程序过早认为 AGV 已空闲，并分配下一项任务。
- 多台 AGV 可能同时占用同一传送带、通道或货架机器人。
- API 状态顺序正确，但物理仿真状态没有同步，最终可能发生碰撞。

根据平台新版帮助，内置调度器还包含以下机制，而我方当前版本尚未完整实现：

- 启动后等待 AGV 归位。
- 每秒读取全部 AGV、传送带和货架机器人状态。
- 只给真正空闲的 AGV 分配任务。
- 对传送带和货架机器人加互斥锁。
- 等待真实放置完成后才将货物标记为 `done`。

## 五、已发现但尚未充分利用的平台能力

### 1. AGV 实时状态接口

平台提供只读接口：

```
http://sim-agv-{1..4}.acoplat-smart-factory.svc.cluster.local:8080/status
```

可用于读取位置、速度、运行状态、目标点、机械臂和载货情况。下一版 Scheduler 应将其作为核心调度输入。

### 2. TDengine 历史回放

平台会保存每台 AGV 每个 tick 的轨迹，包括：

- 坐标和速度
- 目标点
- AGV 状态
- 机械臂状态
- 载货状态
- cargo ID

可用于复盘成功运行和碰撞运行，计算车辆最小距离并定位冲突时刻。

### 3. PathPlan 与 SafetyGuard

平台支持独立的：

- `Policy.PathPlan`
- `Safety.Guard`

但官方允许将路径规划和安全检查内联到 Scheduler 中。现阶段计划先在 Scheduler 内实现资源锁和基础避碰，稳定后再考虑拆分功能块。

### 4. 自定义比赛 Dashboard

平台支持上传自定义前端，可展示真实历史轨迹。后续可以制作调试 Dashboard，辅助分析车辆路径、资源占用和碰撞原因。

## 六、下一阶段工作计划

1. 在 Scheduler 中接入四台 AGV 的 `/status`。
2. 启动后增加归位等待，并确认 AGV 静止。
3. 建立 AGV、传送带和货架机器人的资源占用表。
4. 根据真实位置、速度和负载推进 cargo 状态。
5. 只有确认实际放置完成后才上报 `done`。
6. 先以单车或双车并发确保困难场景稳定完成。
7. 再逐步提高并发度，优化任务分配和总耗时。
8. 连续运行至少三次 Demo Hard，确保 Top3 成绩稳定。
9. 利用 TDengine 轨迹分析最小车距和拥堵点。
10. 稳定后引入路径预约、时间窗或 A*/CBS 等高级算法。

## 七、阶段结论

目前已经完成从代码到正式仿真成绩的全流程验证，并证明我方 Scheduler 能够真实参与平台调度。基础算法在简单场景中运行稳定，也已成功完成过困难场景。

现阶段最核心的问题已经明确：**调度逻辑尚未形成基于真实设备状态的闭环，导致困难场景下存在资源冲突和 AGV 碰撞风险。**

下一阶段不需要继续反复排查平台流程，而应集中完成实时状态轮询、资源互斥和基础避碰，使困难场景从“偶尔成功”提升到“连续稳定成功”。