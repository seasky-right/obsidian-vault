采用两种方法查询三环道路上的出租车点：一种是先选中三环道路，再根据空间关系选择 `TaxiData`；另一种是利用 `LinkID` 进行 SQL 嵌套查询：

```
LinkID IN (
    SELECT LinkID
    FROM BeijingRoad
    WHERE PATHNAME LIKE ![[8个区别点.png]]'%三环%'
)
```

两种方法的查询结果相差 **8 条记录**，其中空间查询比 SQL 嵌套查询多选出 8 个点。根据数据字段定义，`TaxiData.LinkID` 表示 GPS 点所在道路的 ID，而 `BeijingRoad.LinkID` 表示道路 ID，因此 SQL 查询实际上是按照出租车点已有的**道路归属属性**进行判断。

![[8个区别点.png]]
*图：8个差异点的空间位置*

对这 8 条差异记录进一步检查，得到：

| TaxiData LinkID | 差异点数量 | BeijingRoad 中的 PATHNAME | 空间位置特征     |
| --------------- | ----- | ----------------------- | ---------- |
| 109278          | 2     | 空值                      | 道路交叉/连接位置  |
| 113904          | 2     | 空值                      | 十字路口附近     |
| 121501          | 2     | 京承高速公路                  | 三环与高速立交附近  |
| 122770          | 2     | 朝阳北路                    | 三环与朝阳北路交叉处 |
| **合计**          | **8** |                         |            |
|                 |       |                         |            |


![[121501.png]]

![[113904.png]]

![[122770.png]]

![[109278.png]]
*图：4个差异位置的道路属性*

从地图逐点查看可以发现，这些差异点均位于**道路交叉口、立交或连接道路附近**。其中 `121501` 实际归属于“京承高速公路”，`122770` 归属于“朝阳北路”，另外两个 `LinkID` 对应道路的 `PATHNAME` 为空。它们均不满足 SQL 中的：

```
PATHNAME LIKE '%三环%'
```

因此不会被 SQL 嵌套查询选中。

但在交叉口和立交处，一条出租车 GPS 点的几何位置可能恰好位于三环道路与另一道路的交汇位置，因此按照**空间几何关系**进行选择时能够被选中；而 `TaxiData` 中每个点只记录了一个 `LinkID`，其道路匹配结果可能被归到相交道路、匝道或连接段。例如一个位于三环与朝阳北路交叉位置的点，在几何上可以满足与三环道路的空间关系，但其 `LinkID=122770` 所记录的道路归属却是“朝阳北路”，因此空间查询与属性查询得到不同结果。

因此，本次 8 条记录的差异主要反映了两种查询依据的不同：

> **空间查询依据要素之间的几何关系，而 SQL 嵌套查询依据已有的道路编码 `LinkID` 所表示的属性归属。在道路交叉口、立交和连接段等复杂道路结构处，几何关系与唯一的道路归属关系并不一定完全一致，因此产生了少量查询结果差异。**

这 8 个差异点体现了空间数据库中属性关系与空间关系并不完全等价。